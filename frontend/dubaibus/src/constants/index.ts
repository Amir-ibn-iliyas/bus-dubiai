/**
 * Constants Index
 *
 * Single import point for all constants
 * Usage: import { colors, fonts, spacing, routes } from '@/constants';
 */

export { colors, type Colors } from "./colors";
export { fonts, type Fonts } from "./fonts";
export {
  spacing,
  borderRadius,
  type Spacing,
  type BorderRadius,
} from "./spacing";
export { routes, screenTitles } from "./routes";
